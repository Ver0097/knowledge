1. 文档切片方式：
- 自定义中文语义边界（空行、换行、句末标点、停顿标点）
- 使用 bge 原生分词器计算 tokens，确保与模型编码一致
- 优化切片参数
2. 向量检索：
- 向量检索（语义相似度）
- BM25 检索（关键词匹配）
- RRF 融合（智能合并两者结果）
- Reranker 重排序（进一步优化排序）

模型升级后 优化：
1. 关键优化：增加「离线模式强制校验」
当前代码在本地路径不存在时会自动下载，但若你想严格强制离线运行（禁止任何联网行为），需补充 HF_HUB_OFFLINE 环境变量校验：
2. 性能优化：避免重复加载模型
SentenceTransformer 加载完整模型（含权重）比单独加载分词器更耗内存，若你的场景仅需计算 tokens 无需编码向量，可优化为「仅加载分词器」（需补全模型配置文件）